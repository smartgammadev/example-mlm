<script type="text/javascript">
    var secret = "{{ loginSecret(memberExternalId) }}";
    
    function memberLogin(id, secret) {
        $.post("/member/login", { id: id, secret: secret });
    }
    
    $( document ).ready(function() {
            memberLogin('{{ memberExternalId }}', secret);
    });    
</script>